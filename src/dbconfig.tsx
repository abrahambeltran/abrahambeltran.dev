// dbConfig.ts
const config = {
    host: 'localhost',
    user: 'your_username',
    password: 'your_password',
    database: 'your_database',
    port: 5432, // default PostgreSQL port
};

export default config;